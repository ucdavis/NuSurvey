@model NuSurvey.Web.Controllers.CategoryGoalViewModel

@{
	ViewBag.Title = "Create Goal";
}

@section SubNav {
    <ul class="navigation">
        <li>
            <a href="@Url.Action("Index", "Survey")">Surveys</a> 
        </li>
        <li>
            <a href="@Url.Action("Edit", "Survey", new { id = @Model.Survey.Id })">Edit Survey</a> 
        </li>
        <li>
            <a href="@Url.Action("Edit", "Category", new { id = @Model.Category.Id })">Edit Category</a>
        </li>
        <li>
            Create
        </li>
    </ul>
}

@section AdditionalScripts{
     <script type="text/javascript" src='@Url.Content("~/Scripts/jquery.jqEasyCharCounter.js")'></script>
     
         <script type="text/javascript">

             $(document).ready(function () {
                 $("Div.LimitCharacters .text-box").each(function (index, item) {
                     var length = $(item).data("val-length-max");
                     var warnLength = length - 10;
                     $(item).jqEasyCounter({ maxChars: length, maxCharsWarning: warnLength, msgTextAlign: 'left', });
                 });
             });

   </script>
}

@*<h2>Create</h2>*@

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
	@Html.AntiForgeryToken()
	@Html.ValidationSummary(true)
<fieldset>
@* Legend is required if you have more than one widget/unit to help the user understand what is being asked of them.
Example: Dinosaur 1  *@
	@*<legend>CategoryGoalViewModel</legend>*@
	@*<h4>Form Header</h4>*@
	@*<p>Be specific so the user understands what exactly you are looking for.  Example: After Dinosaur 1 moves out of his/her parents' den what will Dinosaur 1 do?  We need to know his employment plan, feeding area, as well as living situation.  This information will not be sold or given to carnivores.</p>*@
	<p>All required fields are in <strong>bold</strong> and have a red <span class="required">*</span> next to the field label.</p>
	<ul>
        @Html.Partial("_CategoryGoals")
		<li>
			@* For the clear and submit buttons an empty label is required. Example: <label>&nbsp;</label> *@
			<label>&nbsp;</label>
			<input class="button" type="submit" value="Submit" /> | @Html.ActionLink("Cancel", "Edit", "Category", new { id = @Model.Category.Id }, new { })
		</li>
	</ul>
</fieldset>
}


